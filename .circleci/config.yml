version: 2.1
jobs:
  compile:
   docker:
      - image: ubuntu:19.10

   steps:
      - run:
          command: |
           apt -y update && apt -y install bc build-essential zip curl libstdc++6 git wget python gcc clang libssl-dev rsync flex bison
           wget https://raw.githubusercontent.com/CurioussX13/.circleci/test/.circleci/build.sh
           sh build.sh
workflows:
  version: 2.1
  cooking:
    jobs:
      - compile
